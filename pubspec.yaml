name: btstream
description: A new Flutter project to get bluetooth data stream and show.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.9+0.9

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  permission_handler: ^8.3.0
  flutter_bluetooth_serial: ^0.4.0
  flutter_svg: ^1.0.0
  lottie: ^1.2.1
  flutter_easyloading: ^3.0.3
  font_awesome_flutter: ^9.2.0
  logger: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/lottiefiles/


